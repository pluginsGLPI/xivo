{
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "glpi-project/tools": "^0.7",
        "php-parallel-lint/php-parallel-lint": "^1.4"
    },
    "provide": {
        "guzzlehttp/guzzle": "*"
    },
    "config": {
        "optimize-autoloader": true,
        "platform": {
            "php": "7.4.0"
        },
        "sort-packages": true
    }
}
